var rotate = function (matrix) {
  const len = matrix.length
  const arr = new Array(len).fill(new Array(matrix.length).fill())
  const flat = matrix.flatMap(item => item)

  for (let i = 0; i < flat.length; i++) {
    // arr[]
  }



}

rotate([
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9]
])
